{
  "name": "db",
  "version": "0.0.0",
  "keywords": [],
  "author": "podvb",
  "license": "ISC",
  "scripts": {
    "start": "npm run server",
    "server": "babel-node server.js --presets env",
    "dev": "nodemon dev.js --exec babel-node --presets env",
    "cluster": "babel-node cluster.js --presets env"
  },
  "dependencies": {
    "bluebird": "^3.5.1",
    "body-parser": "^1.18.2",
    "cluster": "^0.7.7",
    "express": "~4.15.5",
    "live-migration": "0.0.7",
    "pg-promise": "^6.10.2"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.0"
  }
}
